const forbear = @import("forbear");

pub const sidebarBackground = forbear.hex("fafafa");
pub const sidebarText = forbear.hex("364149");
pub const sidebarActive = forbear.hex("008cff");
pub const sidebarBorder = forbear.hex("f4f4f4");

pub const page = forbear.hex("ffffff");
pub const text = forbear.hex("333333");
